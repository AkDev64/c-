#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<char> v = {'a', 'c', 'f', 'd', 'z'};

    // imprimindo o tamanho do vetor, ou seja, quantidade de elementos contidos nele
    cout << "quantidade de elementos do vetor : " << v.size();
  
    return 0;
}
